{
    "buildCommand": "cd ui && pnpm install && pnpm build",
    "outputDirectory": "ui/dist",
    "installCommand": "echo 'dependencies installed inside ui/'",
    "framework": null,
    "rewrites": [
        {
            "source": "/(.*)",
            "destination": "/index.html"
        }
    ]
}